# A stream played on a controller sends nothing the compositor counts as
# activity, so the idle timers would lock the screen, blank the output the
# capture reads from, and suspend the machine while someone is playing on it.
[Service]
ExecStart=
ExecStart=/usr/bin/systemd-inhibit --what=idle --who=Sunshine --why="Streaming host" /usr/bin/sunshine
